# Database
PG_DB=jobserverdb
PG_USER=postgres
PG_PASSWORD=
PG_HOST=localhost
PG_PORT=5432

# SMTP Settings for SendGrid
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587

# SMTP USER is a literal "apikey" for SendGrid SMTP authentication
SMTP_USER=apikey

# Put your full SendGrid API key here (found in SendGrid dashboard → API Keys)
SMTP_PASS=

# Recipient email where alert mails will be sent
ALERT_EMAIL=alertdispatcher@outlook.com

# Verified single sender email address in SendGrid (must be verified)
FROM_EMAIL=alertdispatcher@outlook.com

# Optional Reply-To email address (can be your personal/team mailbox)
REPLY_TO_EMAIL=alertdispatcher@outlook.com





